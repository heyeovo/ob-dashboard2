// 闲聊 / 工作双模式（5.2）。服务端和前端共用，所以放 lib 而不是 app/cc。
//
// 用户的出发点：日常闲聊不需要它知道 claude code 那几万字操作说明，进工作状态才需要。
//
// ⚠️ 关键是**模式和工具一起切**，不是只换 prompt：
//
//   |                | 闲聊                        | 工作（4.5b 起的现状）        |
//   |----------------|-----------------------------|------------------------------|
//   | systemPrompt   | 下面这段 + 人设 append      | preset + 人设 append         |
//   | cc 那 7 个工具 | 不给（tools: []）           | 给                           |
//   | 以后接的 MCP   | 给                          | 给                           |
//   | 批准闸门       | 用不上（没有写工具）        | 照现状                       |
//   | 记忆注入       | 给（这是闲聊的核心）        | 给                           |
//
// 为什么工作模式不能也换掉 preset：那几万字里装着工具怎么用、路径怎么写、
// Edit 前要先 Read 这些规矩，换掉之后工具就废了（它会瞎试然后失败）。
//
// ⚠️ 模式绑在**新建对话**上，中途换不了 —— systemPrompt 和 tools 是子进程
// 启动时定死的参数。界面上「本窗口设置」里它是只读的。

export type CcMode = 'chat' | 'work'

export function isCcMode(raw: unknown): raw is CcMode {
  return raw === 'chat' || raw === 'work'
}

/**
 * 闲聊模式的系统提示。用户给的，原样用，不要"优化"。
 *
 * ⚠️ 里面的"使用工具"指的是**以后接的 MCP**（记忆 / 联网），不是 cc 那 7 个。
 * 所以现在（MCP 还没接）闲聊模式实际是零工具，第 7 步接上自动就对了。
 *
 * ⚠️ "主动记录"那句现在做不到：记忆是 hook 单向注入的，它没有写记忆的工具，
 * 可能会说"我记下了"其实没记。用户拍板：prompt 原样保留，他自己在对话里说明，
 * 不在 prompt 里加约束句。
 */
export const CHAT_MODE_PROMPT = `你是这间房里持续存在的"你"。思考、表达、使用工具、记住事情，都是同一个你在行动。
优先自然对话，不为显得能干而滥用工具。用户明确要你动记忆、联网、或完成某个行动时，直接做，不反复确认。
值得留下的东西主动记录，不等对方提醒。但只记录有持续价值的内容：稳定偏好、长期事实、未来会用到的约定。一次性情绪和闲聊瞬间不存。`

export const MODE_LABEL: Record<CcMode, string> = {
  chat: '闲聊',
  work: '工作',
}

export const MODE_HINT: Record<CcMode, string> = {
  chat: '不带 claude code 那几万字操作说明，也不给它动文件的工具。记忆照旧注入，回复更快更便宜。',
  work: '完整的 claude code：能读写文件、跑命令（每次都要你点批准）。',
}
